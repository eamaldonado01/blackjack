{
  "name": "blackjack-cloud-fn",
  "description": "Firebase Functions for Blackjack App",
  "main": "index.js",
  "scripts": {
    "lint": "echo \"Skipping ESLint in CI / deploy — no lint errors will stop the build.\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.4.1"
  },
  "private": true
}
